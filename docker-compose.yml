services:
  app:
    build: .
    container_name: e14_antiFraude
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=test
    # Terminal interactiva estable para ejecutar comandos dentro del contenedor
    tty: true
    stdin_open: true
    command: sh
